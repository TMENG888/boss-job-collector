# 更新日志

## 1.3.6

- 修复补全链路跑到一半静默中断：单步抛出的未捕获异常会导致
  scheduleNextTick 不执行、闹钟链断裂（需手动点补全JD唤醒）。
  现每步异常被捕获并跳过该条，链路自动继续
- 新增心跳闹钟（每分钟）：闹钟链因任何原因断裂时，1 分钟内
  自动检查并重新拉起（按 enrichScheduled 标志判断是否该跑）
- 浏览器重启后也会自动续跑 JD 补全（纯本地恢复，安全）

## 1.3.5

- JD 补全改为**闹钟驱动的单步状态机**：每步处理一条，然后安排下一次
  chrome.alarms 闹钟。SW 在步与步之间可以被浏览器休眠——闹钟到点会
  把它唤醒继续。彻底解决“浏览器放后台任务就停”：
  ① SW 30s 无事件被杀 → 不再致命，下一步由闹钟拉起
  ② 后台标签页定时器被强节流 → 节奏由 SW 侧闹钟掌控，不再依赖页面定时器
- 窗口最小化时自动无焦点恢复（不抢前台，用户无感），避免后台标签页
  被冻结导致详情页渲染不出内容
- 风控说明：后台运行不改变请求节奏（节奏引擎照常工作），平台无法
  从请求模式区分前台/后台，不增加风控风险
- 注：打包发布后闹钟最小间隔 30s（开发者模式无限制），节奏只会更稳

## 1.3.4

- 修复 IP 级封禁页漏检：封禁页（"访问受限——您的 IP 存在异常行为"）
  标题仍是"BOSS直聘"，旧检测只看标题全部漏过，导致封禁期间继续
  请求加重风控。现正文特征检测（列表页+详情页双处），命中立即停
  止并明确告知用户等解除，已采数据可先导出
- 拟人节奏引擎（反爬核心）：
  - 详情条间间歇 1.2~2.7s → 4~9s，另在详情页停留 1.5~4.5s 模拟阅读
  - 每 5~9 条随机长休 30~90s（真人不会匀速刷几百条）
  - 列表翻页 4~8s → 6~13s，约 1/4 翻页后追加 12~30s 休息
  - 自适应减速：每次被拦截整体速度×1.5（上限4倍），首次拦截即
    冷却 30~60s；连续顺利 15 条缓慢恢复
- 预期节奏：详情约 15~25s/条（含阅读+间歇+长休摊薄），慢但稳

## 1.3.3

- 修复任务静默冻结（72/1000卡住+状态“空闲”的根因）：MV3 Service Worker
  被 Chrome 休眠重启后，activeTabId/运行状态等内存变量丢失，任务无人驱动。
  现 status/activeTabId 全部持久化，SW 唤醒时自动恢复现场：
  任务中 → 重新通知内容脚本继续采集；JD补全中 → 重新调度；
  原标签页丢失 → 自动重新打开搜索页
- 城市直接填名字（如“广州”），代码自动映射，留空=全国；
  仍兼容直接填代码；内置 34 个主要城市
- 弹窗精简：移除提示文字；并发相关陈旧文案一并清理
- 说明：中文逗号切分本来就支持（/[,，、]+/），关键词未接力的真实原因
  是上述 SW 休眠冻结，不是分隔符

## 1.3.2

- 修复提取器结构缺陷：四层降级原本共用一个 try，单层抛异常会导致
  后续层全部跳过、JD 变空（用用户提供的真实详情页 HTML 在 jsdom 中
  实测暴露）。现每层独立隔离，单层故障不影响其余层
- 实测验证：shipped 提取代码在真实详情页 DOM 上层1命中、JD 1120字
  完整提取，薪资/福利/公司块全部正确
- 修复福利标签重复：多个选择器命中同一元素导致同一标签出现3次，现去重
- 新增内容脚本强制注入兑底：PING 探测失败时用 chrome.scripting 手动
  注入 content.js 再试，消灭“扩展重载后孤儿页”导致的未注入失败类
- 新增：本地真实 DOM 回归测试（shipped 代码 + jsdom，详见 CHANGELOG）

## 1.3.1

- 详情提取全链路探针：提取器命中层级（层1容器/层2选择器/层3关键词/层4兑底）
  随结果回传并显示在状态栏；失败时诊断包含 readyState、正文长度、
  job-desc/job-sec-text 元素计数、字体加密标志，控制台同步输出
- 区分两类失败：内容脚本未注入（提示刷新页面重试）vs 已注入但提取为空
  （提取器问题）；后者带详细现场快照
- 提取前先 PING 探测内容脚本注入（最多6×1s），避免把注入慢误判为提取空

## 1.3.0

- 架构重写：JD 详情改为**后台驱动“热”标签页导航**。CDP 实测证明：
  无登录冷请求会被送滑块验证（verify.html）永不自动通过；只有复用
  用户已登录、有交互历史的搜索页标签页做导航，被动安检才会像手动
  点击一样自动通过。新通道：导航进详情 → 提取 → 导航回列表，逐条循环
- 安检页人工介入：导航 25s 未落到详情页时提示用户手动完成滑块，
  完成后自动继续（最长等 2 分钟）
- 移除全部失效通道：fetch 快路径、iframe、新开后台标签页；
  移除内容侧采集池（职责收归后台串行循环，天然防重复）
- 移除“JD详情并发数”选项（串行即最优，一次一条最接近真人）
- 熔断逻辑移至后台；完成态判定不变（只有真拿到 JD 才算完成）

## 1.2.9

- 破案：抓包证实 BOSS 对每次程序化请求都 302 到 JS 安检页
  （passport/zp/security.html，标题“请稍候”，正文“正在加载中”）。
  浏览器中 JS 自动算令牌后跳回详情页，所以手动浏览正常；
  fetch/iframe 无法执行质询，原理上拿不到 JD
- fetch 通道秒级识别安检重定向（via=security），不再白等 8s 解析空壳
- 连续 2 次安检重定向后直接跳过 fetch，全走真实标签页通道
- tab 通道补上安检链等待：开页后先等 URL 从 security.html 落位回
  job_detail（最多25s），再等加载完成、再水合提取；总预算 ~55s
  （TAB_DETAIL 硬超时相应放宽到 90s）
- 安检重定向不计入熔断（tab 通道仍可用，暂停反而浪费）

## 1.2.8

- 修复致命挂起：fetch 超时只保护到响应头，响应体被反爬 tarpit 拖住时
  res.text() 无限挂起（实测卡死在第一条任务毫无进展）。现 abort 定时器
  覆盖到 body 读完为止，超时硬上限 8s
- TAB_DETAIL 通道加 60s 硬超时，任何单条任务都不可能永久卡死 worker
- tab 兜底预算收紧（重试 4→3 次），整体控制在硬超时之内
- 降级走真实详情页时状态栏即时提示，不再是冻结的同一行

## 1.2.7

- 终极兑底：真实标签页导航提取。BOSS 对 fetch/iframe 程序化请求返回空壳
  （实测 JD/福利/行业 91~100% 为空，但页面标题正常），改为后台开真实
  详情页标签页（后台静默，不抢焦点），页面内脚本等水合后提取，用完即关
- 快路径保留：fetch 能拿到内容时仍然最快；拿不到才降级走真实导航
- 详情标签页串行执行（同时最多 1 个），与用户点击行为一致，降低风控关注
- 提取器新增层4兑底：取正文最大岗位词汇文本块，防 class 改版后全军覆没
- 移除已失效的 iframe 兑底通道

## 1.2.6

- 修复：JD 提取为空时仍被标记"完成"（via='iframe' 不校验内容），
  现只有真抓到 JD 才算完成，空结果自动重试（最多3次）
- iframe 慢水合兜底：首次提取为空时再等2秒重新提取一次
- JD 提取器多层降级加固：容器分块拼接 → 直接命中 → 关键词定位
  （不限 class，找职责/要求关键词最紧凑文本块），任何一层异常不中断任务
- 页面指纹诊断：详情提取失败时记录实际返回页面的标题/URL/正文字数，
  状态栏直接显示（如 页面:"BOSS直聘"(1234字)），一眼定位 BOSS 回了什么

## 1.2.5

- 修复：v1.2.4 的拦截检测误判——扫描页面正文导致正常详情页被
  大量误标为"风控拦截"（BOSS 页面 head 本就含 captcha/geetest 等
  SDK 字样）。现只看高置信信号：状态 403/429、<title>、最终 URL
- fetch 超时 12s→8s、iframe 超时 20s→15s，限制真实限流时的单条耗时上限

## 1.2.4

- 风控快速识别：详情请求返回安全验证页/403/429 时立即标记 blocked，
  不再走 iframe 兜底烧满 12s+20s 超时预算（此前被限流时每条耗时约半分钟）
- 熔断器：连续 3 次被拦截即全体暂停 90 秒自动重试，避免硬冲加重风控
- 状态栏实时显示上条耗时与拦截状态（如"上条 850ms"/"⚠风控拦截"）

## 1.2.3

- 修复：JD 任务成功后未清除 detailFetching 领取标记，60s 后被 retryable()
  误判为"超时可重试"，worker 无限循环重抓最早一批已完成任务
  （实测卡死在 9/100 不再推进）；现无论成败均释放标记，存量脏标记自愈

## 1.2.2

- 移除 HR/发布者、HR职位、HR活跃度 三个字段（不再采集与导出）
- 修复 JD 缺漏：职责/任职要求常被拆成多个分块，之前只取第一个分块导致
  约1/3的JD缺"任职要求"段；现改为圈定 JD 模块容器，收集全部分块按文档序去重拼接
- 同步简化：双车道合并回单并发池（慢车道仅为补 HR 而设），HR 校验器/串位检查移除

## 1.2.1

- JD 获取改为双车道架构：快车道（fetch，吃用户并发数）只抓 SSR 内容；
  慢车道（iframe 修 HR）固定 2 路，与快车道流水线并行
- 修复：之前每条任务都因补 HR 而现场开 iframe（整页渲染 + 2.2s 等待），
  高并发时多页同时渲染争抢主线程/连接，吞吐退化成近似单线速度
- 修复：JD 抓取失败时 jd 被清空且不再重试；现改为释放任务限次重试（最多 3 次）
- "补全JD"按钮现在也能补全历史数据中缺失的 HR 字段

## 1.2.0

- JD 详情抓取改为并发池模型，弹窗可自由设置并发数（1~6，默认 1）
- 失败自适应退避：请求异常/超时后自动加长间隔，降低风控风险
- 修复未勾选"采集当前页"时 buildUrl 缺失导致无法启动采集的问题

## 1.1.0

- 逐屏增量采集：每滚动一步即解析上报，达到目标数量立即停止
- 薪资字体加密解密 v2：优先读取薪资元素计算字体定位加密字体，`fonts.check` 验证码位覆盖，
  模板含自定义字体自带 ASCII 字形（PUA 字形常与其同形），并以未分配码位生成豆腐块基准过滤缺字
- JD 详情获取：fetch + DOMParser 解析服务端渲染 HTML，被风控拦截时自动降级同源隐藏 iframe；
  HR 信息（客户端渲染）缺失时用渲染后的 iframe 二次补取
- 翻页等待期间穿插 JD 补全，采集与详情获取并行
- 多关键词队列接力（逗号分隔），突破单搜索词约 300 条的平台上限；严格忠于关键词自身检索结果，不做变体扩展
- HR/时间字段全链路校验（状态标签、公司名串位等异常值自动拦截）+ 弹窗"校验修复"按钮清洗历史数据
- "HR活跃度"列替代 BOSS 已下线的"发布时间"

## 1.0.0

- 首个版本：列表逐屏采集、自动翻页、数量控制、CSV 导出、薪资字体解密（v1）
